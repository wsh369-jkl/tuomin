# Core Framework
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
python-multipart>=0.0.9,<1
pydantic>=2.10,<3
pydantic-settings>=2.2,<3

# Document Processing
pdfplumber>=0.11,<1
pypdfium2>=4.30,<5
Pillow>=10,<13
python-docx>=1.1,<2
PyPDF2>=3.0,<4
pypdf>=4.1,<6
numpy>=1.26,<3
rapidocr>=3.4,<4
onnxruntime>=1.20,<2
opencv-python-headless>=4.10,<5
pdf2docx>=0.5,<1
PyMuPDF>=1.24,<2

# Archive Processing
rarfile>=4.1,<5
py7zr>=0.20,<1

# Database & Utilities
sqlalchemy>=2.0,<3
aiofiles>=23.2,<25
httpx>=0.27,<1
requests>=2.31,<3

# Optional LLM Inference
# 使用 Ollama 时无需额外 Python 依赖
# 高质量低内存模式在 Apple Silicon Mac 上使用本地 Transformers/MLX 推理：
transformers>=5,<6
safetensors>=0.4,<1
tokenizers>=0.22,<0.23
sentencepiece>=0.2,<1
protobuf>=4.25,<7
huggingface_hub>=1.12,<2
torch>=2.5,<3 ; platform_system == "Darwin" and platform_machine == "arm64"
mlx-lm>=0.31,<1 ; platform_system == "Darwin" and platform_machine == "arm64"
llama-cpp-python>=0.3,<1 ; platform_system == "Darwin" and platform_machine == "arm64"
# 模型下载脚本支持 ModelScope/Hugging Face：
modelscope>=1.36,<2
# 如需显式启用 PRIMARY_IE_BACKEND=modelscope，请在隔离环境验证依赖；该路径可能拉取 transformers 4.x，与 mlx-lm 冲突。
# datasets>=3,<4
# simplejson
# sortedcontainers
# 使用 vLLM 时请按显卡与 CUDA 环境单独安装：
# vllm

# Development
pytest>=8.1,<9
pytest-asyncio>=0.23,<1
black>=24.3,<26
